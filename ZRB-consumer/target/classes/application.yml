server:
  port: 80
spring:
  application:
    name: spring-provider-80

# Eureka 配置
eureka:
  instance:
    instance-id: spring-provider-80
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka/,http://localhost:7002/eureka/
